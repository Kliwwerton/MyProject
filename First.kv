
<First>:
    name: 'First'

    BoxLayout:
        padding: 10
        orientation: 'vertical'
        spacing: 5

        Image:
            source: 'Images/image.png'
            center_x: self.parent.center_x
            center_y: self.parent.center_y

        Label:
            text: 'Выберите действие'
            font_size: '30sp'
            size_hint: 1, 0.3
            color: 64/255,64/255,64/255,1

        BoxLayout:
            size_hint: 1, 0.2

            Button:

                Image:
#                    id: image_cow
#                    size: 50, 50
#                    source: 'Images/bull.png'
#                    center_x: self.parent.center_x
#                    center_y: self.parent.center_y

            Label:
                size_hint: 0.7, 1
            Button:
                size_hint: 0.15, 1
                background_color: 0,0,0,0
                on_press:
                    root.print()
#                    root.manager.current = 'Cows_and_bulls'
#                    root.manager.transition.direction = 'down'

#                Image:
#                    id: image_cow
#                    size: 50, 50
#                    source: 'Images/bull.png'
#                    center_x: self.parent.center_x
#                    center_y: self.parent.center_y

        Button:
            id: button_1
            text: 'Расчёт давления прессования\n через удельное.'
            halign: 'center'
            valign: 'middle'
            font_size: '20sp'
#            background_color: 0, 1, 0.8, 1
            size_hint: 1, 0.25
            on_release:
                root.manager.current = 'Second'
#                root.manager.transition.direction = 'left'

        Button:
            id: button_2
            text: 'Расчёт удельного давления\n через прессовое.'
            halign: 'center'
            valign: 'middle'
            font_size: '20sp'
#            background_color: 0 , 1, 0.8, 1
            size_hint: 1, 0.25
            on_release:
                root.manager.current = 'Third'
#                root.manager.transition.direction = 'up'

        Button:
            text: 'Расчёт химического состава.'
            font_size: '20sp'
            size_hint: 1, 0.25
            on_release:
#                app.Fourth.create_new_composition()
                root.manager.current = 'Fourth'

        AnchorLayout:
            size_hint: 1.0, 0.25

#            Button:
#                id: button_guide
#                text: 'СПРАВОЧНИКИ!'
#                font_size: '20sp'
#                size_hint: 1,0.25
    #            on_press:
    #                root.manager.current = 'References'
    #                root.manager.transition.direction = 'left'

        FloatLayout:
            size_hint: 1, 0.15

            Button:
                id: button_exit
                text: 'ВЫХОД'
                font_size: '10sp'
                background_color: 1, 0, 0, 1
                size_hint: .2, 0.8
                pos_hint: {'center_x': 0.5, 'center_y': 0.5}
                on_release:
                    app.close_app()