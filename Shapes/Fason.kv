<Fason>:
    title: 'Расчёт по произвольным данным'
    size_hint: 0.8, 0.45
    auto_dismiss: False
    on_open:
        self.build_instance()

    BoxLayout:
        orientation: 'vertical'
        spacing: 5

        Label:
            id: label_gost_number
            text: ''
            size_hint: 1, 0.15

        BoxLayout:
            size_hint: 1, 0.2

            Label:
                markup: True
                text: 'Площадь изделия, см[sup]2[/sup]: '
                size_hint: 0.65, 1
                halign: 'left'
                valign: 'center'
                text_size: self.size

            TextInput:
                id: square_product
                text: ''
                multiline: False
                size_hint: 0.35, 1
                hint_text: 'Площадь'
                halign: 'center'
                padding_y: [self.height / 2.0 - (self.line_height / 2.0) * len(self._lines), 0]
                input_tipe: 'number'
                input_filter: 'float'

        BoxLayout:
            size_hint: 1, 0.2

            Label:
                markup: True
                text: 'Объём изделия, см[sup]3[/sup]: '
                size_hint: 0.65, 1
                halign: 'left'
                valign: 'center'
                text_size: self.size

            TextInput:
                id: volume_product
                text: ''
                multiline: False
                size_hint: 0.35, 1
                hint_text: 'Объём'
                halign: 'center'
                padding_y: [self.height / 2.0 - (self.line_height / 2.0) * len(self._lines), 0]
                input_tipe: 'number'
                input_filter: 'float'

        BoxLayout:
            size_hint: 1, 0.2

            Label:
                text: 'Вес изделия, грамм: '
                size_hint: 0.65, 1
                halign: 'left'
                valign: 'center'
                text_size: self.size

            TextInput:
                id: weight_product
                text: ''
                multiline: False
                size_hint: 0.35, 1
                hint_text: 'Вес'
                halign: 'center'
                padding_y: [self.height / 2.0 - (self.line_height / 2.0) * len(self._lines), 0]
                input_tipe: 'number'
                input_filter: 'float'

        AnchorLayout:
            size_hint: 1, 0.3

            Button:
                text: 'РАССЧИТАТЬ'
                size_hint: 0.8, 0.6
                pos_hint: {'center_x': 0.5}
                on_press:
                    root.calculation()
                    root.change_text()
                    root.dismiss()

        BoxLayout:
            size_hint: 1, 0.3
            spacing: 5

            AnchorLayout:

                Button:
                    text: 'НАЗАД'
                    font_size: '10sp'
                    size_hint: 0.5, 0.5
                    background_color: 1,0,0,1
                    pos_hint: {'x': 0.6, 'y': 0.5}
                    on_release:
                        root.dismiss()
                        root.return_beck()

            AnchorLayout:

                Button:
                    text: 'ЗАКРЫТЬ'
                    font_size: '10sp'
                    size_hint: 0.5, 0.5
                    background_color: 1,0,0,1
                    pos_hint: {'x': 0.6, 'y': 0.5}
                    on_release:
                        root.dismiss()
